#Bucles#  


# 1. Usa un bucle while para imprimir los nÃºmeros del 1 al 10.
 
number_one = 1 

while number_one :
    print(number_one)
    number_one += 1

    if number_one == 11:      # No need this part actually
        print("Hasta aquí!")
        break

# 2. Usa un bucle for para recorrer la lista[10, 20, 30, 40, 50] e imprime cada nÃºmero.
numeric_list = [10, 20, 30, 40, 50]

for element in numeric_list: #In the correctin he use number instead of element
    print(element)

# 3. Escribe un programa que use un bucle while para sumar los nÃºmeros del 1 al 100 e imprime el resultado.
#I do understand , about It demand , do the sum of all numbers betwen 1 to 100 . 
#Add the list of all numbers from 1 to 100.

resultado =  1                  
sum = 0 #Correction 
while resultado <= 100: 
    sum += resultado #Correction
    resultado += 1
print(sum)  # It is sum instead of resultado . 

# 4. Escribe un bucle for que imprima cada carÃ¡cter de la cadena "Python".

word = "Python"

for element in word : #Correction use "for letter" not "for element"
    print(element)

# 5. Usa un bucle while para encontrar el primer nÃºmero divisible por 7 entre 1 y 50.
#I need 3 variables : number seven , the list of 1 to 50 and the operation of it
i = 1        
while i <= 50:        #All this is from the correction , because I did really wrong hahahah
    if i % 7 == 0:
        print(i)
        break
    i += 1 


# 6. Usa un bucle for para recorrer el diccionario {"name": "Brais", "age": 37, "country": "Galicia"} e imprime las claves.

dictionary = {"name": "Brais", "age": 37, "country": "Galicia"}

for key in dictionary : 
    print(key)
    
# 7. Escribe un programa que use un bucle while para imprimir los nÃºmeros pares entre 1 y 20 . 

one_twenty = list(range(1, 21))

while one_twenty :
    print(one_twenty)
    break

# 8. Usa un bucle for con la funciÃ³n range() para imprimir los nÃºmeros del 1 al 10 en orden inverso. 

for element in reversed(range(1, 11)):
    print(element)

# 9. Escribe un programa que use un bucle for para contar cuÃ¡ntas veces aparece el nÃºmero 30 en la lista[30, 10, 30, 20, 30, 40].
treinta = [30, 10, 30, 20, 30, 40]
treinta_count = 0
for number in treinta : 
    if number == 30:
        treinta_count +=1
print(treinta_count)     # This is like to make a list.conunt() , but manually I think hahhahah 

# 10. Usa un bucle for para recorrer una lista de nombres y detener el bucle cuando se encuentre el nombre "Brais".

nombres = ["Pepe", "Forajido" , "Jhon", "Brais" , "Jesucrist"]

for element in nombres :
   if element == "Brais" :
    print(element)


    



    
     
        
     

    
    


    
    

    
    

